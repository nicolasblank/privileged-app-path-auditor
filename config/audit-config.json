{
  "$schema": "./schema/audit-config.schema.json",
  "description": "Runtime configuration for the Privileged App Path Auditor. These settings control thresholds, exclusions, and behavior. Override values here to customize for your environment.",
  "version": "0.2.0",
  "lastUpdated": "2026-03-15",
  "stalePrivilege": {
    "inactiveDays": 90,
    "description": "Number of days without a sign-in before an app is considered stale"
  },
  "filters": {
    "excludeDisabledApps": false,
    "excludeFirstPartyMicrosoftApps": true,
    "excludeAppIds": [],
    "description": "Filter settings. excludeFirstPartyMicrosoftApps uses the merill/microsoft-info database (4000+ known app IDs, refreshed daily) plus appOwnerOrganizationId to identify and skip Microsoft first-party apps."
  },
  "consentRisk": {
    "flagUserConsentedApps": true,
    "description": "When true, ConsentRisk mode will enumerate apps that received user consent (not admin consent)"
  },
  "credentialHygiene": {
    "preferredCredentialTypes": ["Certificate", "FederatedIdentityCredential"],
    "riskyCredentialTypes": ["Password"],
    "flagMultipleCredentials": true,
    "flagExpiredCredentials": true,
    "description": "Credential type risk classification. Certificates and federated credentials are preferred over secrets (passwords)."
  },
  "output": {
    "maxUsersInRoleAuditList": 500,
    "truncateAfter": 50,
    "description": "Controls output size. Users beyond truncateAfter are summarized as '... (N more)' in console output. Full data is always included in CSV exports."
  }
}
